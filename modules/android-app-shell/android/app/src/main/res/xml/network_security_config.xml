<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for HWMS

    Development: Allows all cleartext (HTTP) traffic for local development.
    Production: Change base-config cleartextTrafficPermitted to "false"
                and add specific domain-config for your API servers.
-->
<network-security-config>
    <!-- Development: Allow all cleartext traffic -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
        Production example: Restrict to specific domains only
        Uncomment and modify for production use:

        <base-config cleartextTrafficPermitted="false">
            <trust-anchors>
                <certificates src="system" />
            </trust-anchors>
        </base-config>

        <domain-config cleartextTrafficPermitted="false">
            <domain includeSubdomains="true">your-api-domain.com</domain>
        </domain-config>
    -->
</network-security-config>
